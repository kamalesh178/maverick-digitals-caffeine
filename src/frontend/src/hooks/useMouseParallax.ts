/**
 * useMouseParallax — stub. Mouse parallax removed per user request.
 * Kept for import compatibility.
 */
export function useMouseParallax() {
  return { mouseX: 0, mouseY: 0, rawX: 0, rawY: 0 };
}

export function useTilt(_intensity = 10) {
  return { rotateX: 0, rotateY: 0, style: {} };
}
