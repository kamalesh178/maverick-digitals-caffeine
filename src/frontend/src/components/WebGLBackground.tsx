// WebGLBackground — removed per user request (CSS-only animations instead).
// This stub is kept for import compatibility while the Three.js layer is fully removed.

export function WebGLBackground() {
  return null;
}
